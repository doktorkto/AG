#include <iostream>
#include <vector>
using namespace std;

int main(){
    int m,n;
    cin >> m >> n;
vector<vector<int>> g(n);
for (int i = 0; i < m; i++){
int v, u;
cin >> v >> u;
g[--v].push_back(u);
g[u].push_back(v);
}
}
