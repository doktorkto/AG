#include <stdio.h>
#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    bool*b=new bool[n+1];
    b[0]=1;
    b[1]=1;
    for (int i=2; i<n+1;i++){
        if (b[i]==0) {
            for (int j=i*2; j<n+1; j+=i){
                b[j]=1;
            }
        }
    }
    for (int i=0;i<n+1;i++){
        if (b[i]!=0) cout <<i<<" ";
    }
}
